[build]
  command = "pnpm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  AUTH_TRUST_HOST = "true"
  SEED_DEMO = "true"
  SECRETS_SCAN_OMIT_PATHS = ".env.example,CLAUDE.md,docs/**"
  SECRETS_SCAN_OMIT_KEYS = "BOOTSTRAP_ADMIN_EMAIL,NEXTAUTH_URL"
